1、核心交换机的配置
（1）、问题1：跨vlan开机，需要核心交换机转发，但由于目标计算机没有启动，就不知道目标IP所对应的MAC地址，就不
能把开机的唤醒包发到对应的电脑。
解决办法：在核心配置arp命令，将目标IP和其MAC地址绑定
（2）、问题2：由于计算没有启动，核心交换机没有学习到MAC地址
解决办法：利用mac-address命令进行静态绑定，将MAC和vlan id、接口绑定

2、二层交换机的配置
在链路上的二层交换机，配置和核心交换机中的问题2的解决办法同理。